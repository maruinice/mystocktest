1. Celery Worker (任务执行器) - 必需
Celery Worker负责执行后台任务，包括：
股票基础数据同步
日线行情数据同步
增量数据同步
历史数据清理
数据质量报告生成
2. Celery Beat (定时任务调度器) - 必需
Celery Beat负责定时触发任务：
每天早上8点同步股票基础数据
每个交易日下午3点半同步当日行情
每小时执行增量数据同步
每周日凌晨清理历史数据
每天凌晨生成数据质量报告
3. Flower (可选，但推荐)
Flower是Celery的Web监控界面，可以查看任务执行状态。

方案一：使用Docker Compose（推荐）

# 进入后端目录
cd /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading

# 启动所有Docker服务（包括MySQL、Redis、Celery Worker、Celery Beat、Flower）
docker-compose up -d

# 单独启动Flask和WebSocket（需要在conda环境中）
conda activate stock_trading
nohup python run_flask.py --host 0.0.0.0 --port 5000 > flask.log 2>&1 &
nohup python start_websocket.py > websocket.log 2>&1 &

# 启动前端
cd ../stock-ai-frontend/
nohup npm run dev > frontend.log 2>&1 &


方案二：手动启动所有服务（适合开发调试）

# 1. 启动MySQL和Redis
cd /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading
docker-compose up -d mysql redis

# 2. 激活conda环境
conda activate stock_trading

# 3. 启动Flask API
nohup python run_flask.py --host 0.0.0.0 --port 5000 > flask.log 2>&1 &
echo "Flask PID: $!"

# 4. 启动WebSocket
nohup python start_websocket.py > websocket.log 2>&1 &
echo "WebSocket PID: $!"

# 5. 启动Celery Worker（重要！）
nohup celery -A app.tasks.celery_app worker --loglevel=info > celery_worker.log 2>&1 &
echo "Celery Worker PID: $!"

# 6. 启动Celery Beat（重要！）
nohup celery -A app.tasks.celery_app beat --loglevel=info > celery_beat.log 2>&1 &
echo "Celery Beat PID: $!"

# 7. 启动Flower监控（可选）
nohup celery -A app.tasks.celery_app flower --port=5555 > flower.log 2>&1 &
echo "Flower PID: $!"

# 8. 启动前端
cd ../stock-ai-frontend/
nohup npm run dev > frontend.log 2>&1 &
echo "Frontend PID: $!"


📋 验证服务状态

# 检查所有服务端口
sudo lsof -i :3306  # MySQL
sudo lsof -i :6379  # Redis
sudo lsof -i :5000  # Flask API
sudo lsof -i :8765  # WebSocket
sudo lsof -i :5555  # Flower
sudo lsof -i :5173  # 或 :3000 前端

# 查看日志
tail -f /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading/flask.log
tail -f /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading/websocket.log
tail -f /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading/celery_worker.log
tail -f /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading/celery_beat.log

🛑 停止所有服务脚本

#!/bin/bash
# stop_all_services.sh

echo "正在停止所有服务..."

# 停止Python进程
pkill -f run_flask.py
pkill -f start_websocket.py
pkill -f "celery.*worker"
pkill -f "celery.*beat"
pkill -f "celery.*flower"

# 停止前端
pkill -f "npm run dev"

# 停止Docker服务
cd /home/meiming/source_code/xl_ai_stock_trading/stock_ai_trading
docker-compose down

echo "所有服务已停止"


🌐 访问地址
前端应用: http://localhost:5173 或 http://localhost:3000
后端API文档: http://localhost:5000/apidocs/
WebSocket: ws://localhost:8765
Flower监控: http://localhost:5555
MySQL: localhost:3306
Redis: localhost:6379