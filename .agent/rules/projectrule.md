---
trigger: always_on
---

## 1. 确保生产级标准

- 代码必须确保生产级标准
- 所有API都使用真实数据库
- 完整的事务管理和错误处理
- 无Mock数据或硬编码

## 2. 回复语言与时间格式

- 所有回复、文档、日志、文档必须**使用中文**。
- 时间格式统一使用 **Asia/Shanghai** 时区，格式为：`yyyy-MM-dd HH:mm:ss`  
  示例：`2025-11-19 14:30:25`

## 3. 系统与环境说明

- 当前开发系统：**Ubuntu 24.04 LTS**， **Conda** 环境 `stock_trading`
- 容器编排方式：统一使用 **docker compose**（新版写法，不带连字符）

4. 记录开发过程中遇到的问题和对应的解决方法，按照从上到下顺序追加记录在issue/solution-{yyyy-MM-dd}.md 中。

5. 每次对话完成后的总结或者报告按照从上到下顺序追加记录在summary/summary-{yyyy-MM-dd}.md 中，要求简洁明了，不需要长篇大论。