---
alwaysApply: true
---

以下为本项目统一的代码、开发、记录规范

## 1. 确保生产级标准

- 代码必须确保生产级标准
- 所有API都使用真实数据库
- 完整的事务管理和错误处理
- 无Mock数据或硬编码

## 2. 回复语言与时间格式

- 所有回复、文档、日志、文档必须**使用中文**。
- 时间格式统一使用 **Asia/Shanghai** 时区，格式为：`yyyy-MM-dd HH:mm:ss`  
  示例：`2025-11-19 14:30:25`

## 3. 系统与环境说明

- 当前开发/服务器系统：**Ubuntu 24.04 LTS**， **Conda** 环境 `stock_trading`
- 容器编排方式：统一使用 **docker compose**（新版写法，不带连字符）  
  错误写法：`docker-compose up`  
  正确写法：`docker compose up`

## 4. 问题与解决方案记录规范

- 所有开发、调试、部署过程中遇到的问题及最终解决方法（不含总结），**必须当天记录**
- 记录位置：`issue/solution-{yyyy-MM-dd}.md`
- 记录方式：**从上到下顺序追加**（一天一个文件，多条记录垂直追加在同一文件中）
- 文件命名示例：`solution-2025-11-19.md`

## 5. 对话结束后总结与报告记录规范

- 每次与AI或其他成员的重要对话完成后，必须整理输出**总结或阶段性报告**
- 记录位置：`summary/summary-{yyyy-MM-dd}.md`
- 记录方式：**从上到下顺序追加**（一天一个文件，多条记录垂直追加在同一文件中）
- 文件命名示例：`summary-2025-11-19.md`